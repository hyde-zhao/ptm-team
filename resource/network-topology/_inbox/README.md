# Inbox — 待入库文件

本目录存放待规范化入库的原始组网拓扑文件（如 Visio 导出的拓扑图、初步设计的 Mermaid 图等）。

## 入库流程

1. 将新的组网拓扑源文件放入此目录
2. 参照 `topology-spec.md` 规范化格式
3. 追加到 `topology-collection.md`
4. 更新 `index.yaml`
5. 将已加工完成的源文件移出 inbox 或标记为 `processed`
