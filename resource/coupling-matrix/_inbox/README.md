# Inbox — 待入库文件

本目录存放待规范化入库的原始耦合矩阵文件（如 Excel、导出的 CSV 等）。

## 当前状态

原始文件位于顶层 `.input/` 目录：
- `NGFW系列 V60R1C00 特性树&耦合矩阵&形态差异三表.xlsx`

已加工完毕入库，原始文件保留在 `.input/` 作为溯源。

## 入库流程

1. 将新的耦合矩阵源文件放入此目录
2. 运行加工脚本或手动编写对应的 YAML
3. 更新 `index.yaml`
4. 将已加工完成的源文件移出 inbox 或标记为 `processed`
