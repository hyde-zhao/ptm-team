<!--
  模板用途：feature-parser Skill 在 input 阶段完成后，
           向用户展示并请求确认目录结构的标准输出格式。
  触发节点：ptm-tde 12步流程 → 步骤1 (input) 完成后
  对应文件：.output/feature-input/directory-structure.md
           .output/feature-input/raw-requirements.md
-->

# {特性名} — 特性解析确认

> **用户确认点 1/5 — 目录结构确认**
> 请确认以下解析结果是否正确，确认后进入场景发现阶段。

---

## 解析来源

| 项目 | 内容 |
|------|------|
| 原始文件 | `{文件名}.{扩展名}` |
| 需求条目总数 | {N} 条 |
| 模块数（四级）| {M} 个 |
| 子模块数（五级）| {K} 个 |

---

## 三~五级目录结构

```
{特性名}（三级）
├── {模块A}（四级）
│   ├── {子模块A1}（五级）—— {关联SR数}条需求
│   ├── {子模块A2}（五级）—— {关联SR数}条需求
│   └── {子模块A3}（五级）—— {关联SR数}条需求
├── {模块B}（四级）
│   ├── {子模块B1}（五级）—— {关联SR数}条需求
│   └── {子模块B2}（五级）—— {关联SR数}条需求
└── {模块C}（四级）
    └── {子模块C1}（五级）—— {关联SR数}条需求
```

---

## 结构化需求列表（摘要）

> 完整列表见 `.output/feature-input/raw-requirements.md`

| 编号 | 所属模块 | 子模块 | SR 名称 | 描述摘要 |
|------|---------|--------|---------|---------|
| SR-001 | {模块A} | {子模块A1} | {名称} | {描述前50字}... |
| SR-002 | {模块A} | {子模块A1} | {名称} | {描述前50字}... |
| SR-003 | {模块A} | {子模块A2} | {名称} | {描述前50字}... |
| ... | ... | ... | ... | ... |

**缺失/歧义项**（如有）：

| 编号 | 问题描述 | 处理方式 |
|------|---------|---------|
| SR-XXX | 模块归属不明确 | 暂归入"{推断模块}"，请确认 |

---

## 确认选项

> 请选择以下操作之一：

1. ✅ **确认通过** — 目录结构正确，保存并进入场景发现（步骤2）
2. ✏️ **修改模块/子模块** — 请描述需要重命名、合并或拆分的模块
3. ➕ **补充缺失模块** — 请描述需要新增的模块或子模块
4. 🔄 **重新解析** — 原始文件理解有误，请重新上传或说明结构规则

---

<!-- ptm-tde 内部记录，用户确认后写入 -->
**确认状态**：⬜ 待确认 / ✅ 已确认
**确认时间**：
**用户备注**：
