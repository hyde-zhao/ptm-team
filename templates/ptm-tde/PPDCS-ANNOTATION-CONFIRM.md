<!--
  模板用途：m-analyzer Skill 在 m-analysis 阶段完成后，
           向用户展示并请求确认 PPDCS 特征标注的标准输出格式。
  触发节点：ptm-tde 12步流程 → 步骤3 (m-analysis) 完成后
  对应文件：.output/m-analysis/ppdcs-annotation.md
           .output/m-analysis/test-points.md
-->

# {特性名} — PPDCS 特征标注确认

> **用户确认点 3/5 — PPDCS 特征标注确认**
> 请确认以下特征标注是否准确，标注结果将直接驱动后续设计方法推荐（步骤7）。

---

## 统计摘要

| 维度 | 数量 |
|------|------|
| 子模块总数（五级目录节点）| {N} |
| 有测试点的子模块数 | {M} |
| M 分析测试点总数 | {K} |
| 需求覆盖率（初检）| {X}% |

### PPDCS 特征分布

| PPDCS 特征 | 子模块数 | 占比 | 对应设计方法 |
|-----------|---------|------|------------|
| P-Process（流程）| {N} | {X}% | 流程图法 |
| P-Parameter（参数）| {M} | {Y}% | 判定表法 |
| D-Data（数据）| {K} | {Z}% | 等价类+边界值法 |
| C-Combination（组合）| {J} | {W}% | 组合法（Pairwise）|
| S-State（状态）| {L} | {V}% | 状态图法 |
| 混合特征 | {H} | {U}% | 主方法+辅方法 |

---

## PPDCS 标注详表

> 每行对应一个五级目录节点（单功能），含主特征、辅特征和判定依据。

| 模块（四级）| 子模块（五级）| PPDCS 主特征 | 辅特征 | 判定依据 |
|-----------|------------|-------------|--------|---------|
| {模块A} | {子模块A1} | **P-Parameter** | D-Data | 多参数（IP/端口/协议）间存在规则约束，影响最终判定结果 |
| {模块A} | {子模块A2} | **P-Process** | — | 配置步骤有明确顺序约束（格式校验→名称检查→保存），不可回退 |
| {模块B} | {子模块B1} | **S-State** | — | 任务对象在"未启动/运行中/完成/失败"间可互转，存在状态生命周期 |
| {模块B} | {子模块B2} | **D-Data** | C-Combination | 查询条件有独立取值范围；当条件数≥4时可升级为 Combination |
| {模块C} | {子模块C1} | **C-Combination** | — | 5个因子各有3~4状态，全组合=120+，需Pairwise压缩 |
| ... | ... | ... | ... | ... |

---

## 区分说明（如有混合特征）

| 子模块 | 疑似特征 | 区分依据 | 最终判定 |
|--------|---------|---------|---------|
| {子模块X} | Process vs State | 流程步骤是否可回退？→ 不可回退 | P-Process |
| {子模块Y} | Parameter vs Data | 参数间是否有规则？→ 有规则约束 | P-Parameter |
| {子模块Z} | Data vs Combination | 因子是否独立？→ 独立但数量多 | C-Combination |

---

## 测试点生成统计

| 模块（四级）| 子模块数 | 测试点数 | 需求覆盖 |
|-----------|---------|---------|---------|
| {模块A} | {N} | {M} | {K}/{K_total} SR ✅ |
| {模块B} | {N} | {M} | {K}/{K_total} SR ✅ |
| {模块C} | {N} | {M} | ⚠️ {K}/{K_total} SR（待补充）|

**未覆盖需求**（如有）：

| SR 编号 | 描述 | 原因 | 建议 |
|---------|------|------|------|
| SR-XXX | {描述} | {原因} | 补充测试点 |

---

## 确认选项

> 请选择以下操作之一：

1. ✅ **确认通过** — PPDCS 标注准确，保存 `ppdcs-annotation.md`，继续步骤4（F分析）
2. ✏️ **修改特征标注** — 请指出子模块名称及希望修改的特征（主特征/辅特征/判定依据）
3. ➕ **补充测试点** — 请指出未覆盖的需求编号，m-analyzer 补充后重新展示
4. 🔀 **调整目录结构** — 发现目录层级需要调整，需先回到步骤1重新确认目录

---

<!-- ptm-tde 内部记录，用户确认后写入 -->
**确认状态**：⬜ 待确认 / ✅ 已确认
**确认时间**：
**用户备注**：
