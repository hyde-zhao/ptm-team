<!--
  模板用途：scenario-discovery Skill 在 scenario 阶段完成后，
           向用户展示并请求确认应用场景列表的标准输出格式。
  触发节点：ptm-tde 12步流程 → 步骤2 (scenario) 完成后
  对应文件：.output/scenarios/confirmed-scenarios.md

  两层结构说明：
  - 层1 场景原理：解释场景为何存在、解决什么问题、适用范围
  - 层2 使用路径：用户从输入到输出的全路径，每个节点含操作+观察点，粒度粗于LC但覆盖所有主路径
-->

# {特性名} — 应用场景确认

> **用户确认点 2/5 — 应用场景确认**
> 请确认以下场景是否完整准确，确认后进入 M 分析（步骤3）。

---

## 信息来源

| 来源 | 命中场景数 |
|------|----------|
| MCP 知识库 | {N} 个 |
| Web 搜索 | {M} 个 |
| 用户提供材料 | {K} 个 |
| **合计** | **{总数} 个候选场景** |

---

## 场景概览

| 编号 | 场景名称 | 分类 | 主路径数 | 关联模块 | 来源 |
|------|---------|------|---------|---------|------|
| SCN-{特性缩写}-001 | {场景名} | 主场景 | {N}条 | {模块A},{模块B} | MCP |
| SCN-{特性缩写}-002 | {场景名} | 主场景 | {N}条 | {模块B} | Web |
| SCN-{特性缩写}-003 | {场景名} | 扩展场景 | {N}条 | {模块A} | 用户材料 |
| ... | ... | ... | ... | ... | ... |

---

## 场景详情

### SCN-{特性缩写}-001: {场景名称}

**分类**：主场景 / 扩展场景

---

#### 层1：场景原理

| 字段 | 内容 |
|------|------|
| **业务背景** | {为什么需要这个场景，业务驱动因素，2~3句} |
| **核心目标** | {用户期望解决的问题或达到的目标} |
| **适用范围** | {哪种设备型号/版本/配置前提下适用} |
| **不适用** | {明确排除的情况} |
| **关联模块** | {四级目录 > 五级目录} |

---

#### 层2：使用路径

> 以下路径覆盖该场景的主要用户行为链路，粒度粗于逻辑用例，但确保主路径完整。
> 每个节点描述：用户操作 OR 系统处理 + 用户在该点可观察到的内容。

##### 主路径 P1：{路径名称，如"正常配置并生效"}

```
输入 → [N1] → [N2] → [N3] → ... → 输出
```

| 节点 | 类型 | 操作描述 | 观察点（用户能看到什么） | 预期状态 |
|------|------|---------|----------------------|---------|
| N1 | 用户输入 | {用户提供什么：命令/配置/数据} | {界面/回显/日志中可见的内容} | {期望的中间状态} |
| N2 | 系统处理 | {系统执行什么处理逻辑} | {处理过程中用户可观察的指标/状态} | {期望的处理结果} |
| N3 | 用户操作 | {用户的下一步操作} | {操作后立即可观察的反馈} | {期望状态} |
| N4 | 系统输出 | {系统最终输出什么} | {最终产物/结果的可观察形式} | {期望的终态} |

##### 扩展路径 P2：{路径名称，如"配置错误时的错误处理"}

| 节点 | 类型 | 操作描述 | 观察点 | 预期状态 |
|------|------|---------|--------|---------|
| N1 | 用户输入 | {异常或边界输入} | {系统反馈} | {错误处理状态} |
| N2 | 系统处理 | {系统如何处理异常} | {错误提示/日志} | {终止或降级处理} |

##### 扩展路径 P3：{路径名称，如"并发或多设备场景"}（可选）

<!-- 同上格式 -->

---

### SCN-{特性缩写}-002: {场景名称}

<!-- 同上两层结构 -->

#### 层1：场景原理

| 字段 | 内容 |
|------|------|
| **业务背景** | |
| **核心目标** | |
| **适用范围** | |
| **不适用** | |
| **关联模块** | |

#### 层2：使用路径

##### 主路径 P1：{路径名称}

| 节点 | 类型 | 操作描述 | 观察点 | 预期状态 |
|------|------|---------|--------|---------|
| N1 | | | | |

---

## 覆盖分析

| 模块（四级）| 有场景覆盖 | 路径数 | 关联场景编号 |
|-----------|----------|--------|------------|
| {模块A} | ✅ | {N}条路径 | SCN-001(P1,P2), SCN-002(P1) |
| {模块B} | ✅ | {N}条路径 | SCN-001(P1) |
| {模块C} | ⚠️ 无场景 | 0 | — |

> ⚠️ 无场景覆盖的模块：请确认是否需要补充，或确认该模块无典型应用场景可忽略。

---

## 确认选项

> 请选择以下操作之一：

1. ✅ **全部确认** — 场景列表完整，写入 `confirmed-scenarios.md`，进入步骤3（M分析）
2. ✏️ **修改场景** — 请指出场景编号及修改内容（名称/路径节点/关联模块）
3. ➕ **新增场景** — 请描述需要补充的业务场景（业务背景+关键路径）
4. 🗑️ **删除场景** — 请指出需删除的场景编号及原因
5. ➕ **补充路径** — 请指出场景编号，补充缺失的扩展路径
6. 🔄 **重新搜索** — 请补充更多搜索关键词，重新检索

---

<!-- ptm-tde 内部记录，用户确认后写入 -->
**确认状态**：⬜ 待确认 / ✅ 已确认
**确认时间**：
**用户备注**：
