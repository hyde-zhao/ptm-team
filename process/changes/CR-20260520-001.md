---
change_id: CR-20260520-001
workflow_id: WF-PTM-TEAM-20260520-001
created_at: "2026-05-20T14:43:38+08:00"
created_by: meta-po
status: closed
impact_level: medium
rollback_to: story-execution
approval_source: user-request
---

# CR-20260520-001 - Improve Scenario Discovery Rediscovery Flow

## Change Request

用户要求改进 ptm-tde 顶层 `skills/scenario-discovery/SKILL.md`。当前 skill 会把 `analysis/scenarios/scenario-analysis.md` 这类 functional scenario seed 直接整理成最终输出，缺少“场景再发现 / 头脑风暴 / 重构”阶段。目标是让 skill 能从 functional scenario seed 重组为类似 `analysis/scenarios/scenario-deployment-policy-route.md` 的部署型场景发现输出件。

## Five-Dimension Impact Analysis

| Dimension | Impact | Decision |
|---|---|---|
| Requirement | 影响 scenario-discovery 的输入识别、场景再发现、范围收敛、部署型输出质量规则。 | 原文档更新：直接修订 `skills/scenario-discovery/SKILL.md`，保留既有能力并增加 functional seed 到 deployment scenario 的 rediscovery 规则。 |
| Design | 影响 Skill 执行流程、输出结构、atomic-ops、Topology、Knowledge Reference、Confirmation Gaps 和质量检查。 | 原文档更新：在同一 Skill 内整合，避免新增共享模板体系。 |
| Story / Implementation | 影响单个 Skill 文件；可能影响 `skills/README.md` 的适用/调用说明。 | 修改 `skills/scenario-discovery/SKILL.md`；评估并视需要同步 `skills/README.md`。 |
| Security / Permission | 读取输入材料和 TGFW 组网集合，均为本地只读；不得虚构组网、凭据或可执行atomic-ops。 | 新增禁止脑补、不得自行发明组网、atomic-ops 引用规则，避免伪造能力。 |
| Delivery | 影响交付 Skill 行为说明，不涉及安装脚本或平台路径。 | 无需修改安装器；Markdown 结构和关键规则用轻量命令验证。 |

## Document Handling Decisions

| Document | Decision | Baseline Retention |
|---|---|---|
| `skills/scenario-discovery/SKILL.md` | 原文档更新 | 保留现有准确优先、Topology、atomic-ops、Knowledge Reference、Tool Draft、Confirmation Gaps 语义，增量加入再发现和部署场景输出规则。 |
| `skills/README.md` | 不变 | 当前文件只有占位内容 `Skills 使用帮助，待补充`，没有可同步的 Skill 索引、适用范围表或调用说明；本次不引入不对称单项说明。 |
| `process/STATE.md` | 新增/更新 | STATE.md 不存在，按用户要求初始化最小运行状态。 |
| `process/handoffs/CR-20260520-001-meta-dev-scenario-discovery.md` | 新增 | 记录给开发线程的最小上下文和调度证据。 |

## Affected Scope

- Primary file owner: `skills/scenario-discovery/SKILL.md`
- Secondary file owner: `skills/README.md`
- Runtime governance files: `process/STATE.md`, `process/changes/CR-20260520-001.md`, `process/handoffs/CR-20260520-001-meta-dev-scenario-discovery.md`
- Reference inputs if present:
  - `analysis/scenarios/scenario-deployment-policy-route.md`
  - `input/TGFW测试组网图集合.md`
  - `analysis/scenarios/scenario-analysis.md`

## Execution Plan

1. Create minimal runtime state and this CR.
2. Create a meta-dev handoff with the exact requested Skill changes and dispatch evidence fields.
3. Run a development execution thread through parent-session native `spawn_agent` and record dispatch evidence.
4. Verify by searching required headings and hard rules with `rg`; run lightweight Markdown structure checks where practical.
5. Update this CR with execution conclusion, README decision, and pending confirmations.

## Approval / Execution Conclusion

| Field | Value |
|---|---|
| approval_status | approved-for-execution |
| approved_by | user request in current conversation |
| execution_status | completed |
| dispatch_mode | native spawn_agent |
| dispatch_evidence | `meta-dev` agent `019e4423-fea7-7132-9196-32197e7882b3` (`dev-kong`) completed implementation; see `process/handoffs/CR-20260520-001-meta-dev-scenario-discovery.md`. Earlier `codex exec` subprocess was stopped before product-file changes. |
| product_files_changed | `skills/scenario-discovery/SKILL.md` |
| readme_decision | `skills/README.md` unchanged because it is currently a placeholder with no stable Skill index or invocation table to update. |
| pending_confirmation | Whether a project-confirmed `analysis/scenarios/scenario-deployment-policy-route.md` exists in the target project remains an execution-time input for this Skill; no such file exists in this repository. |

## Dispatch Correction

At 2026-05-20T14:48:28+08:00 the user corrected that the parent session has native `spawn_agent`. The earlier `codex exec` subprocess was stopped and must not be used for this CR. Product files `skills/scenario-discovery/SKILL.md` and `skills/README.md` had no diff after stopping it.

Parent session dispatched native `meta-dev` after this correction. `dev-kong` completed the product-file implementation and verification.

## Implementation Result

| Field | Value |
|---|---|
| completed_at | 2026-05-20T14:53:29+08:00 |
| implementation_agent | `meta-dev` / `dev-kong` |
| implementation_agent_id | `019e4423-fea7-7132-9196-32197e7882b3` |
| changed_files | `skills/scenario-discovery/SKILL.md` |
| unchanged_files | `skills/README.md` |

Implemented capabilities:

- 输入文档类型识别：`raw_requirement` / `functional_scenario_seed` / `deployment_scenario_draft` / `confirmed_scenario_artifact`。
- functional scenario seed 必须进入场景再发现与头脑风暴，禁止直接转最终场景，禁止 seed one-to-one rewriting。
- 范围收敛与 `out_of_scope_candidates`，包括用户声明仅 IPv4 时过滤 IPv6 的规则。
- 项目目标形态学习，优先参考 `analysis/scenarios/scenario-deployment-policy-route.md` 的结构。
- Seed-to-Scenario Mapping、TGFW 组网集合规则、Topology Catalog、Topology Source Mapping 和策略路由默认拓扑选择。
- atomic-ops 规则，直接引用 `op_id`，不虚构中间编号。
- Scenario Details 与 ptm-tde Scenario Discovery Structured Supplement 双层输出。
- 策略路由专项头脑风暴维度、硬规则和输出质量检查清单。

## Verification

| Command | Result |
|---|---|
| `rg -n "输入文档类型识别|场景再发现|Seed-to-Scenario|TGFW测试组网图集合|atomic-ops|功能初稿不可直接|输出质量检查" skills/scenario-discovery/SKILL.md` | passed |
| `git diff --check -- skills/scenario-discovery/SKILL.md skills/README.md` | passed |
| `rg -n '```' skills/scenario-discovery/SKILL.md` | no matches; no code fences to leave unclosed |
| `rg -n "node2_dut1_tg1_link3|node4_dut2_tg1_sw2_link7|node2_dut1_tg1_link5|node3_dut1_tg1_pppoe_link4|out_of_scope_candidates|Scenario Details|Structured Supplement|confirmation_gaps|Knowledge Reference" skills/scenario-discovery/SKILL.md` | passed |

## Validation Result

| Field | Value |
|---|---|
| cp6_status | PASS |
| cp6_result | `process/checks/CP6-CR-20260520-001-scenario-discovery-CODING-DONE.md` |
| cp7_status | PASS |
| cp7_result | `process/checks/CP7-CR-20260520-001-scenario-discovery-VERIFICATION-DONE.md` |
| qa_agent | `meta-qa` / `qa-hua` |
| qa_agent_id | `019e4435-64e3-7050-9a0a-9908f3dff537` |
| verified_at | 2026-05-20T15:10:44+08:00 |
| remaining_user_decision | Final approval and whether `.gitignore` belongs in the same commit as `skills/scenario-discovery/SKILL.md`. |

## User Review Feedback - 2026-05-20T15:35:36+08:00

用户反馈：将 atomic-ops 修改为原子操作，即 atomic-ops。

处理结论：

- 本反馈属于 CR-20260520-001 范围内的口径收敛，不新建独立 CR。
- `skills/scenario-discovery/SKILL.md` 已从“REST API / CLI / tool-method / atomic-ops 并列作为引用类型”调整为“atomic-ops 是唯一原子操作引用对象”。
- REST API、CLI、tool-method 只能作为 atomic-op 的底层 `invoke_contract`、`observe_contract` 或实现通道描述，不再作为 `source_type` 输出。
- 既有 CP7 是反馈前结果；本次补改已完成本地轻量验证，若继续走完整门禁则应重新生成 CP6/CP7 或追加复核记录。

本地轻量验证：

| Command | Result |
|---|---|
| `rg -n 'atomic-ops 是唯一|source_type.*atomic-ops|不得将 \`rest-api\`|atomic-op 的 \`target\`|REST API、CLI、tool-method 只能作为 atomic-op' skills/scenario-discovery/SKILL.md` | passed |
| `rg -n '\| \`source_type\` \|.*(rest-api|cli-tool|tool-method)' skills/scenario-discovery/SKILL.md || true` | no output; `source_type` table no longer lists REST/CLI/tool-method |
| `git diff --check -- skills/scenario-discovery/SKILL.md .gitignore` | passed |

## Flow-Wide Atomic-Ops Sweep - 2026-05-20T15:43:00+08:00

用户反馈：检查整个流程中是否还有旧独立引用对象表述，并全部修改为 atomic-ops。

处理结论：

- 已对 `agents/`、`docs/`、`skills/` 下产品文件执行全量检索。
- 正式文案中不再保留旧独立引用对象名。
- `action_source_refs` 字段名保留，作为 trace chain 兼容字段；字段语义已统一改为 atomic-ops `op_id` 引用。
- 示例中的旧 `AS-*` 引用已替换为 `fw_*` 风格 atomic-ops `op_id`；剩余 `DAS-*` 均为设备型号，不属于atomic-ops。
- REST API、CLI、tool-method 只保留为 atomic-op 底层调用/观测契约说明，不作为 `source_type` 或独立引用类型。

本地轻量验证：

| Command | Result |
|---|---|
| `rg -n -i "atomic-ops|atomic-ops|atomic-ops|atomic-ops" . -g '!process/**' -g '!build/**' -g '!*.egg-info/**' || true` | no output |
| `rg -n 'AS-[A-Z0-9]' agents docs skills \| rg -v 'DAS-\|assets/自动化工厂资产清单\|ngfw-install\|traffic-skill' || true` | no output |
| `rg -n '\| \`source_type\` \|.*(rest-api|cli-tool|tool-method)' agents docs skills || true` | no output |
| `git diff --check -- agents docs skills .gitignore` | passed; Git emitted CRLF normalization warnings for three pre-existing CRLF files, but no whitespace errors |

## Scenario Self-Check Supplement - 2026-05-20T15:56:39+08:00

检查结论：需要补充。

原因：

- `scenario-discovery` 的输出质量检查已经覆盖再发现、Topology、atomic-ops 和缺口，但 CP02 检查点规范仍偏“人工确认场景列表”，缺少自动场景自检门。
- `checkpoint-manager` 只明确实现 CP01 input 自检，未维护 CP02 scenario 自检规则。
- README / checkpoint spec 未要求检查 functional seed 是否经过再发现、Seed-to-Scenario Mapping、Topology Catalog、atomic-ops 唯一口径和 confirmation gaps 分类。

已补充：

- `skills/checkpoint-manager/SKILL.md`：新增 CP02 Scenario 场景自检，包含 Entry Criteria、Checklist、Exit Criteria、Deliverables、Gotchas。
- `docs/ptm-tde/checkpoint-spec.md`：将 CP02 从 manual 扩展为 auto + manual，并新增 12 项自动自检和 7 项人工确认项。
- `docs/ptm-tde/README.md`：补充 CP02 自动自检内容和人工确认内容。
- `docs/ptm-tde/USER-MANUAL.md`：更新 scenario-discovery 输入说明，明确 atomic-ops 能力说明与底层契约边界。

本地轻量验证：

| Command | Result |
|---|---|
| `rg -n "CP02 Scenario|场景自检|Seed-to-Scenario|Topology Catalog|atomic-ops 唯一|输入文档类型识别|功能种子再发现|缺口分类|CP02_scenario_auto" skills/checkpoint-manager/SKILL.md docs/ptm-tde/checkpoint-spec.md docs/ptm-tde/README.md docs/ptm-tde/USER-MANUAL.md` | passed |
| `rg -n -i "<旧独立引用对象关键词集合>" agents docs skills process -g '!*.egg-info/**' || true` | no output |
| `git diff --check -- agents docs skills .gitignore` | passed; Git emitted CRLF normalization warnings for three pre-existing CRLF files, but no whitespace errors |

## Operation Path Modeling Feedback - 2026-05-21T09:46:50+08:00

用户反馈：按照建议继续修改 ptm-tde 场景发现相关的 skill 和流程，重点补强操作路径建模、正常路径必要性、异常路径追溯和结构化补充一致性。

处理结论：

- 本反馈仍属于 `CR-20260520-001` 范围内的 scenario-discovery 收敛项，不新建 CR。
- `skills/scenario-discovery/SKILL.md` 已新增 Operation Path Modeling 规则，要求 `normal_path` 包含 `step_id / sub_step_ids / operation / necessity / description`。
- `normal_path.necessity` 固定为 `必要 / 可选 / 至少选择一项`；选择组必须保留可选子步骤和不能全部跳过的约束。
- `abnormal_path` 已升级为可追溯结构，要求 `abnormal_item / related_normal_steps / input_or_state / expected_handling`，异常项必须追溯到正常路径步骤或说明来源。
- `minimal_logic_chain` 必须保留可选步骤和 `至少选择一项` 语义，不得改写成线性必做链路。
- 策略路由场景补充 `forwarding action` 与 `match dimensions` 拆分建模规则，防止把“配置策略路由”写成不可拆分的大步骤。
- CP02 自动自检和人工确认口径已同步到 `checkpoint-manager`、`checkpoint-spec`、README、USER-MANUAL、runtime artifacts、component manual、skill references 和 `ptm-tde` 主 Agent。

本轮产品文件变化：

| File | Change |
|---|---|
| `skills/scenario-discovery/SKILL.md` | 新增操作路径建模、异常路径追溯、选择语义保留、策略路由动作拆分、硬规则、质量检查和验收项。 |
| `skills/checkpoint-manager/SKILL.md` | CP02 自检新增正常路径可追溯、选择组完整、异常路径可追溯和最小逻辑链一致性检查。 |
| `docs/ptm-tde/checkpoint-spec.md` | CP02 自动/人工检查项同步 Operation Path 与 Abnormal Path。 |
| `docs/ptm-tde/README.md` | 主流程和 CP02 自检/人工确认说明同步操作路径口径。 |
| `docs/ptm-tde/USER-MANUAL.md` | 场景模型和 scenario-discovery 使用说明同步 `normal_path` / `abnormal_path` 字段契约。 |
| `docs/ptm-tde/runtime-artifacts.md` | 新增场景产物字段契约，明确 Operation Path 与异常追溯。 |
| `docs/ptm-tde/component-manual.md` | 关键调用关系补充 Operation Path。 |
| `docs/ptm-tde/skill-references.md` | scenario-discovery 职责补充 Operation Path。 |
| `agents/ptm-tde.md` | scenario 阶段和 CP02 用户确认点补充 Operation Path、Seed-to-Scenario Mapping 和 CP02 自检结果。 |

本地轻量验证：

| Command | Result |
|---|---|
| `rg -n "Operation Path|操作路径|normal_path|abnormal_path|related_normal_steps|necessity|至少选择一项|minimal_logic_chain|Seed-to-Scenario|CP02|atomic-ops 唯一" skills/scenario-discovery/SKILL.md skills/checkpoint-manager/SKILL.md docs/ptm-tde/checkpoint-spec.md docs/ptm-tde/README.md docs/ptm-tde/USER-MANUAL.md docs/ptm-tde/runtime-artifacts.md docs/ptm-tde/component-manual.md docs/ptm-tde/skill-references.md agents/ptm-tde.md` | passed |
| `git diff --check -- skills/scenario-discovery/SKILL.md skills/checkpoint-manager/SKILL.md docs/ptm-tde/checkpoint-spec.md docs/ptm-tde/README.md docs/ptm-tde/USER-MANUAL.md docs/ptm-tde/runtime-artifacts.md docs/ptm-tde/component-manual.md docs/ptm-tde/skill-references.md agents/ptm-tde.md` | passed |

待刷新：

- CP6 coding done：已刷新，`PASS`。
- CP7 verification done：已刷新；产品契约验证 `PASS`，用户已确认 `process/VALIDATION-ENV.yaml` 中的 `approval.confirmed=true`，正式 CP7 为 `PASS`。

## CP6 / CP7 Refresh Result - 2026-05-21T09:57:32+08:00

| Gate | Status | Evidence | Notes |
|---|---|---|---|
| CP6 coding done | PASS | `process/checks/CP6-CR-20260520-001-scenario-discovery-CODING-DONE.md`; `meta-dev` agent `019e4837-a8be-7ac3-9b0c-8099d5370b76` | 覆盖本轮 9 个产品文件；`rg`、`git diff --check`、Markdown fence parity 检查通过；meta-dev 未修改产品文件。 |
| CP7 verification done | PASS | `process/checks/CP7-CR-20260520-001-scenario-discovery-VERIFICATION-DONE.md`; `meta-qa` agent `019e483b-df7a-71b1-8f1c-dad446d65ff5`; `process/VALIDATION-ENV.yaml` | 产品契约验证 PASS；用户回复“同意”后已记录 `approval.confirmed=true`，正式 CP7 放行。 |

CP7 产品验证已通过的范围：

- `normal_path` 字段契约：`step_id / sub_step_ids / operation / necessity / description`。
- `necessity` 枚举：`必要 / 可选 / 至少选择一项`。
- `至少选择一项` 选择组：列出可选子步骤、不能全部跳过，并保留到 `minimal_logic_chain`。
- `abnormal_path.related_normal_steps`：异常项追溯到正常路径步骤/子步骤，无法追溯时说明来源并进入缺口或风险说明。
- 策略路由 `forwarding action` 与 `match dimensions` 拆分。
- CP02 自动自检和人工确认口径。
- `ptm-tde` 主 Agent、README、USER-MANUAL、runtime artifacts、component manual、skill references 与 Skill/CP02 的一致性。

用户确认：

- 2026-05-21T10:36:04+08:00 用户回复“同意”。
- 已创建 `process/VALIDATION-ENV.yaml` 并记录 `approval.confirmed=true`。
- 已基于当前 CP7 只读验证证据刷新 CP7 为 `PASS`。

下一步：

- CR 已按用户请求关闭。

## Closure - 2026-05-21T10:39:19+08:00

| Field | Value |
|---|---|
| closure_status | closed |
| closed_by | user request |
| closed_at | 2026-05-21T10:39:19+08:00 |
| close_command | 关闭CR |
| implementation_status | completed |
| cp6_status | PASS |
| cp7_status | PASS |
| git_commit | `c4c7460 完善 ptm-tde 场景发现操作路径建模` |
| git_push | `origin/main` |

关闭结论：

- 本 CR 覆盖的 scenario-discovery 再发现、操作路径建模、CP02 自检和 ptm-tde 流程同步已完成。
- 产品文件已提交并推送到 GitLab。
- 运行态 CR、CP6、CP7、VALIDATION-ENV 和 handoff 记录保留在 `process/`，不提交 GitLab。
- 当前无阻断项、无待确认项。
